[package]
name = "tests"
version.workspace = true
edition.workspace = true

[dependencies]
macros = { path = "../macros" }
core = { path = "../core" }
storage = {path = "../storage"}
backends = { path = "../backends" }